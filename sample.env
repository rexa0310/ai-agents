# Secrets only. All other configuration lives in config.yaml.

OPENROUTER_API_KEY=""

# MONGO_URI enables the document registry; leave empty to disable.
# May contain credentials, so it lives here rather than in config.yaml.
MONGO_URI=""
# MONGO_URI="mongodb://localhost:27017"
